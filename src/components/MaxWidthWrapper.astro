---
interface Props {
  id?: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<div
  id={id}
  class:list={[
    "mx-auto w-full max-w-[1400px] px-5 sm:px-6 md:px-10",
    className,
  ]}
>
  <slot />
</div>
